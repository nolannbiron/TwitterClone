export interface Gif{
    id: string;
    type: string
}

export interface ImageAttachment{
    name: string;
    size: number;
    lastModifiedDate: Date,
    lastModified: number,
    url: string;
}